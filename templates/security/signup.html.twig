{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
<div class='block'>
    <h2 class="pb-30">Insccription</h2>
    {{form_start(form)}}
    {{form_rest(form)}}
    <button class="btn btn-primary">Inscription</button>
    <small>
        <a href="{{ path('login')}}">Vous avez deja un compte ?</a>
    </small>
    
    {{form_end(form)}}
</div>

{% endblock %}
